Developers: Brad Turner, Benjamin Hogan

For a log of the chat protocols to check if its working, open ../logs folder and there is a text document 
which is written to after each session.

When a new client connects after the last has left the server, the server will have to enter some text to pass the scanner on.

Works with localhost
